import React from 'react';
import imgLogo from '../../img/logo.png';

const Logo = () => {
    return (
        <div style={{textAlign: 'center', margin: '0 0 35px'}}>
            <img src={imgLogo} alt="" />
        </div>
    );
};

export default Logo;